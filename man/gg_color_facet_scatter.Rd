% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_color_facet_scatter.R
\name{gg_color_facet_scatter}
\alias{gg_color_facet_scatter}
\title{Colored point plot (scatter-plot) with facets}
\usage{
gg_color_facet_scatter(df, x_var, y_var, col_var = NULL, facet_var = NULL, ...)
}
\arguments{
\item{df}{input dataset (tibble or data.frame)}

\item{x_var}{x variable (supplied to \code{ggplot2::aes(x = )})}

\item{y_var}{y variable (supplied to \code{ggplot2::aes(y = )})}

\item{col_var}{color variable (supplied to \code{ggplot2::geom_point(ggplot2::aes(color = ))})}

\item{facet_var}{facet variable (supplied to \code{ggplot2::geom_point(ggplot2::aes(color = ))})}

\item{...}{other arguments passed to (\code{ggplot2::facet_wrap(vars())})}
}
\value{
A \code{ggplot2} plot object
}
\description{
Colored point plot (scatter-plot) with facets
}
\examples{
require(ggplot2)
diamonds <- ggplot2::diamonds
mini_dmnds <- diamonds[sample(nrow(diamonds), size = 1000), ]
gg_color_facet_scatter(
  df = mini_dmnds,
  x_var = "carat",
  y_var = "price",
  col_var = "cut",
  facet_var = "cut",
  alpha = 1 / 3,
  size = 2
)
# compare with
ggplot2::ggplot(
  data = mini_dmnds,
  mapping = ggplot2::aes(x = carat, y = price)
) +
  ggplot2::geom_point(aes(color = cut, group = cut),
    size = 2, alpha = 1 / 3
  ) +
  ggplot2::facet_wrap(. ~ cut) +
  ggplot2::theme_minimal() +
  ggplot2::theme(legend.position = "bottom")

gg_color_facet_scatter(
  df = mini_dmnds,
  x_var = "carat",
  y_var = "price",
  col_var = "cut",
  facet_var = NULL,
  alpha = 1 / 3,
  size = 2
)
# compare with
ggplot2::ggplot(
  data = mini_dmnds,
  mapping = ggplot2::aes(x = carat, y = price)
) +
  ggplot2::geom_point(aes(color = cut, group = cut),
    size = 2, alpha = 1 / 3
  ) +
  ggplot2::theme_minimal() +
  ggplot2::theme(legend.position = "bottom")
gg_color_facet_scatter(
  df = mini_dmnds,
  x_var = "carat",
  y_var = "price",
  col_var = NULL,
  facet_var = NULL,
  alpha = 1 / 3,
  size = 2
)
# compare with
ggplot(
  data = mini_dmnds,
  mapping = ggplot2::aes(x = carat, y = price)) +
  ggplot2::geom_point(size = 2, alpha = 1 / 3) +
  ggplot2::theme_minimal() +
  ggplot2::theme(legend.position = "bottom")
}
